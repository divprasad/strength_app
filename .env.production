# Production environment — used by `npm run build` and `npm start`
# Prisma resolves `file:./strength_dairy.db` relative to prisma/schema.prisma
# → resolves to prisma/strength_dairy.db
# Docker overrides this via docker-compose.yml environment block (takes precedence)
DATABASE_URL="file:./strength_dairy.db"
