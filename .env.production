# Production environment — used by `npm run build` and `npm start`
# Prisma resolves `file:./strength_diary.db` relative to prisma/schema.prisma
# → resolves to prisma/strength_diary.db
# Docker overrides this via docker-compose.yml environment block (takes precedence)
DATABASE_URL="file:./strength_diary.db"
